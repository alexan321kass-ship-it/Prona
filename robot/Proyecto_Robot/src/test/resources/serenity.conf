serenity {
    take.screenshots = FOR_EACH_ACTION
    console.colors = true
    logging = VERBOSE
}

webdriver {
    driver = chrome
    autodownload = false
    chrome.driver = "C:/Users/janus/Desktop/BACKUOv2/chromedriver/chromedriver-win64/chromedriver.exe"
    wait.for.timeout = 10000
    capabilities {
        "goog:chromeOptions" {
            args = ["start-maximized", "--remote-allow-origins=*"]
        }
    }
}